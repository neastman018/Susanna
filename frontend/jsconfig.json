{
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"],
      // NEW ALIAS: Allows imports like: import config from '@app-config/config.json'
      "@app-config/*": ["../config.json"]
    }

    
  }
}
